# Sailboat Routing System - Environment Configuration

# Production Domain Name (e.g. routing.yourdomain.com)
# Set to 'localhost' for local development with internal self-signed TLS
DOMAIN=localhost

# Optional: Email address for Let's Encrypt account registration & expiry notices
ACME_EMAIL=admin@example.com
